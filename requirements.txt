# sxw_optimization_demo — Python 3.12 运行依赖精确锁
# 安装：.venv/bin/pip install -r requirements.txt
# 镜像兜底：-i https://pypi.tuna.tsinghua.edu.cn/simple --trusted-host pypi.tuna.tsinghua.edu.cn
# 只列项目直接运行依赖；平台相关的传递依赖不进入 release identity。

# --- Agent runtime (Google ADK) ---
google-adk[a2a]==2.6.2           # 仅启用项目实际使用的 A2A 可选集成
a2a-sdk==1.1.2                   # A2A 1.0 SDK；ADK 通过兼容层承接现有 JSON-RPC 链路
litellm==1.95.0                  # ADK LiteLlm provider，项目亦直接引用其异常类型
google-genai==2.16.0             # FunctionDeclaration / Content / Part 等公开类型

# --- Web / SSE ---
fastapi==0.141.1
starlette==1.3.1                 # common.obs 直接使用 middleware/request/response
uvicorn[standard]==0.52.1
python-multipart==0.0.32         # multipart 上传（多模态图片/文件）
aiosqlite==0.22.1                # Canonical Runtime / RAG SQLite（显式 SQL）

# --- HTTP / config ---
httpx==0.28.1
pydantic==2.12.5                 # current DTO / Runtime contract
pydantic-settings==2.14.2
python-dotenv==1.2.2             # run_all 与各 BaseSettings 共享同一 .env/真实环境变量解析语义
PyYAML==6.0.3                    # Claude SKILL frontmatter 安全解析
jsonschema==4.23.0               # Draft 2020-12 ToolCatalog / Native arguments

# --- LLM / embeddings ---
openai==2.53.0                   # 嵌入与单轮文本直连 DashScope compatible-mode

# --- RAG storage (local backends) ---
numpy==2.5.1                     # 本地向量余弦
rank_bm25==0.2.2                 # 本地全文 BM25
jieba==0.42.1                    # 中文分词
